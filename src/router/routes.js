// import store from '@state/store'; // in case we have to authenticate and redirect.
import Home from '@views/Home.vue';

export default [
  {
    path: '/',
    name: 'home',
    component: Home,
  },
];
